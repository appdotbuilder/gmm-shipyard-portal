import { type Project } from '../schema';

export async function getProjects(): Promise<Project[]> {
    // This is a placeholder declaration! Real code should be implemented here.
    // The goal of this handler is fetching all projects for the public gallery.
    // Should return projects ordered by display_order.
    return [];
}

export async function getFeaturedProjects(): Promise<Project[]> {
    // This is a placeholder declaration! Real code should be implemented here.
    // The goal of this handler is fetching only featured projects for the homepage.
    // Should return featured projects ordered by display_order.
    return [];
}